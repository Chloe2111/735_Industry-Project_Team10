spring.application.name=backend
server.port=${PORT:8080}

# Real value comes from the MONGO_URI environment variable, or from
# application-local.properties (git-ignored) via the "local" profile.
# See application-local.properties.example for setup instructions.
spring.data.mongodb.uri=${MONGO_URI:mongodb://127.0.0.1:27017/client_portal}
